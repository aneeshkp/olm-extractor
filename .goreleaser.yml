version: 2

project_name: olm-extractor

before:
  hooks:
    - go mod tidy

builds:
  - id: bundle-extract
    main: ./cmd/main.go
    binary: bundle-extract
    env:
      - CGO_ENABLED=0
    goos:
      - windows
      - linux
      - darwin
    goarch:
      - amd64
      - arm64
    ignore:
      - goos: windows
        goarch: arm64
    ldflags:
      - -X 'github.com/lburgazzoli/olm-extractor/internal/version.Version={{.Version}}'
      - -X 'github.com/lburgazzoli/olm-extractor/internal/version.Commit={{.Commit}}'
      - -X 'github.com/lburgazzoli/olm-extractor/internal/version.Date={{.Date}}'

archives:
  - format: tar.gz
    name_template: '{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}'
    format_overrides:
      - goos: windows
        format: zip
    files:
      - LICENSE*
      - README*

checksum:
  name_template: '{{ .ProjectName }}_{{ .Version }}_checksums.txt'
  algorithm: sha256

release:
  draft: false
  prerelease: auto
  mode: append
  header: |
    ## Release {{ .Tag }}

changelog:
  disable: true
